# web_ui/src

Frontend source code.

## Suggested structure
- components (chat list, message row, badges)
- state (socket connection, buffering, virtualization)
- formatting (emotes, links, mentions)
- safety (client-side redaction, link warnings)
