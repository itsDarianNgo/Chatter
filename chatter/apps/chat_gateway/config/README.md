# chat_gateway/config

Runtime configuration for the gateway.

## Examples
- environment-specific settings (dev/stage/prod)
- bus/stream connection settings
- rate/broadcast batching thresholds
- safety policy toggles

Keep secrets out of repo; use `.env` / secret manager in deployment.
