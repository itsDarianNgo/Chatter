# stream_context/src

Implementation of stream ingestion and context extraction.

## Suggested components
- audio ingestion + ASR adapter
- optional frame sampler + caption adapter
- event detection (heuristic/ML)
- context aggregator + publisher
- health and telemetry hooks
