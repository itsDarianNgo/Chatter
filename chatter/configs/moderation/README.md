# configs/moderation

Moderation and safety policy configuration.

## Examples
- blocklists and regex patterns
- redaction rules (PII)
- allowed emotes list
- link handling policy

Used by `packages/safety/` and enforced in the gateway.
