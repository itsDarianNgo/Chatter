# configs/prompts

Shared prompt fragments and global rules.

## Purpose
Centralize non-persona-specific prompt policies such as:
- “one message only”
- max length guidance
- universal safety constraints
- formatting rules (Twitch-like)

Used by `apps/persona_workers/` prompt assembly.
