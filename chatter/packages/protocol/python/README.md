# protocol/python

Python bindings for canonical schemas.

## Responsibilities
- Pydantic models (or dataclasses) that mirror JSON Schemas
- Validation helpers and parsing utilities

Used by Python apps (stream_context, persona_workers).
