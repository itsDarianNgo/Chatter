# protocol/typescript

TypeScript bindings for canonical schemas.

## Responsibilities
- Type definitions (and optional runtime validators)
- Helpers for safely decoding websocket payloads

Used by `apps/web_ui` and any TS-based services.
